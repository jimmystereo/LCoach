package com.luntianji.l_coach.tools;

import android.content.Context;
import android.widget.ArrayAdapter;

import androidx.annotation.NonNull;

public class TrainingSpinnerAdapter extends ArrayAdapter<CharSequence> {
    public TrainingSpinnerAdapter(@NonNull Context context, int resource) {
        super(context, resource);
    }
}
